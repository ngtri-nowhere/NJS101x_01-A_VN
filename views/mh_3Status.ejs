<%- include('./head.ejs') %>
    <link rel="stylesheet" href="../css/mh_3.css">
    </head>

    <body>
        <%- include('./navigation.ejs') %>
            <main>
                <div class="name__employee">
                    <h3>Name:</h3>
                    <p>
                        <%= prods.name %>
                    </p>
                </div>
                <div class="info__employee">
                    <p>Số Ngày Làm: <% if(pr !=null) { %>
                            <%= pr.length %> ngày
                                <% } else { %>
                                    Tháng này không có dữ liệu
                                    <% } %>
                    </p>
                    <p>Thời gian bắt đầu:<% if(pr !=null) { %>
                            <%= userStartTime %>
                                <% } else { %>
                                    Tháng này không có dữ liệu
                                    <%} %>
                    </p>
                    <p>Thời gian kết thúc: <% if(userEndTime !=null) { %>
                            <%=userEndTime %>
                                <%} else { %>
                                    Tháng này không có dữ liệu
                                    <% } %>
                    </p>
                    <p>Số giờ được tính làm thêm: <% if(prodTime !=null) { %>
                            <%= prodTime %> giờ
                                <% } else { %>
                                    Tháng này không có dữ liệu
                                    <% } %>

                    </p>
                    <p>Lương tháng: <% if(pr !=null) { %>
                            <%= salary %> vnđ <% } else { %>
                                    Tháng này không có dữ liệu
                                    <% } %>
                    </p>
                </div>
            </main>

            <%- include('./end.ejs') %>